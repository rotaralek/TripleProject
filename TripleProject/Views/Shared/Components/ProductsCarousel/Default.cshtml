@model IEnumerable<TripleProject.Areas.Admin.Models.Product>

<div class="owl-carousel owl-carousel-mono mb-4">
    @foreach (var item in Model)
    {
        string image = item.Image != null ? item.Image.Path : null;

        <div>
            <div class="square-image part-50" style="background-image: url(@image)"></div>
            <div class="card-img-overlay-bottom">
                <h3><a asp-controller="Home" asp-action="AdvertisementsSingle" asp-route-id="@item.Id">@item.Title</a></h3>
                <p>@item.Price</p>
                <p>@item.Quantity</p>
                <p>@item.Catalog.Name</p>
                <a asp-controller="Home" asp-action="AdvertisementsSingle" asp-route-id="@item.Id" class="btn btn-primary">Buy now</a>
            </div>
        </div>
    }
</div>