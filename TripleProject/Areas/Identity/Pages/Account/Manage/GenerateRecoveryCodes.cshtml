@page
@model GenerateRecoveryCodesModel
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@inject IHtmlLocalizer<SharedResource> SharedLocalizer
@{
    ViewData["Title"] = SharedLocalizer["Generate two-factor authentication (2FA) recovery codes"];
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

@Html.Partial("_StatusMessage", Model.StatusMessage)
<h4>@ViewData["Title"]</h4>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="material-icons">warning</span>
        <strong>@SharedLocalizer["Put these codes in a safe place"].</strong>
    </p>
    <p>
        @SharedLocalizer["If you lose your device and don't have the recovery codes you will lose access to your account"].
    </p>
    <p>
        @SharedLocalizer["Generating new recovery codes does not change the keys used in authenticator apps. If you wish to change the key used in an authenticator app you should"] <a asp-page="./ResetAuthenticator">@SharedLocalizer["reset your authenticator keys"].</a>
    </p>
</div>
<div>
    <form method="post" class="form-group">
        <button class="btn btn-danger" type="submit">@SharedLocalizer["Generate Recovery Codes"]</button>
    </form>
</div>